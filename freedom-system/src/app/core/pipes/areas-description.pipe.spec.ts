import { AreasDescriptionPipe } from './areas-description.pipe';

describe('AreasDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new AreasDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
