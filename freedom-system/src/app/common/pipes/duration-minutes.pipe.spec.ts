import { DurationMinutesPipe } from './duration-minutes.pipe';

describe('DurationMinutesPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationMinutesPipe();
    expect(pipe).toBeTruthy();
  });
});
